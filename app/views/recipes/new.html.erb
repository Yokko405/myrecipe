<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>レシピ投稿</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
    <h1>レシピ投稿</h1>
    <div class="container">
        <%= form_for :recipe, url: recipes_path do |f| %>
            <div class="field">
                <%= f.label :name, "レシピ名" %>
                <%= f.text_field :name %>
            </div>
            <div class="field">
                <%= f.label :ingredients, "材料" %>
                <%= f.text_area :ingredients %>
            </div>
            <div class="field">
                <%= f.label :instructions, "調理手順" %>
                <%= f.text_area :instructions %>
            </div>
            <%= f.submit "投稿" %>
        <% end %>
    </div>
</body>
</html>
